<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels1="clr-namespace:RetroEngine.Editor.Core.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="RetroEngine.Editor.Core.Views.ProjectOpenView">
    <Design.DataContext>
        <viewModels1:ProjectOpenViewModel/>
    </Design.DataContext>

    <TabControl TabStripPlacement="Left" x:DataType="viewModels1:ProjectOpenViewModel" ItemsSource="{Binding Tabs}">
        <TabControl.ItemTemplate>
            <DataTemplate DataType="viewModels1:ProjectOpenViewTab">
                <TextBlock Text="{Binding Header}"/>
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>
            <DataTemplate DataType="viewModels1:ProjectOpenViewTab">
                <ContentControl Content="{Binding Content}"/>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</UserControl>
